Where options include:
    Filter rules: '^' means exclusion.
                   Multiple items are separated by ','.
                   Default includes all.
    -c <classFilter>
                   Filter rules for classes.
    -m <methodFilter>
                   Filter rules for methods.
    -i <constructorFilter>
                   Filter rules for constructors.
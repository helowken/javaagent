#!/bin/bash

if [ "$1" = "-i" ]
then
    export main_class="agent.builtin.tools.ResultCalculator\$CostTimeByInvoke"
    shift
else
    export main_class="agent.builtin.tools.ResultCalculator\$CostTimeByChain"
fi

$(dirname "$0")/calculate_result $*

Usage: cost_time_result [-options] inputPath
${filterOptions.txt}
    ${chainMatchFilterOptions.txt}
    -r <ratesRange>
                   An range of rates to calculate distribution of avgTime.
                   e.g., "0.7, 0.9, 0.95"

    Expr:           Attributes that can use in expr are "count", "avgTime" and "maxTime". Time unit is "ms".
                   e.g., "avgTime>=1 && count > 10000".
    ${resultCommon.txt}